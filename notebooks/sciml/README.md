# Scientific ML Notebook Layout

The `sciml/` directory is organized by model family:

- `operators/` — DeepONet and FNO operator-learning notebooks
- `spatial/` — convolutional field-to-field surrogates
- `pinns/` — physics-informed neural network notebooks
- `generative/` — conditional diffusion notebooks
